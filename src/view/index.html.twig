<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>MineSweeper</h1>
    <a href="?reset=1">New Game</a>
    <div class="error">{{error}}</div>
    <div style="--size:{{ mineSweeper.tiles|length}}" class="minesweeper">
    {% for x in 0..mineSweeper.tiles|length -1 %}
        {% for y in 0..mineSweeper.tiles.0|length -1 %}
            <a href="/?x={{x}}&y={{y}}" class="{{ mineSweeper.tiles[y][x] != 0 and mineSweeper.tiles[y][x] != constant('App\\MineSweeper::BOMB') ? 'hidden' }}">
            {{ mineSweeper.tiles[y][x] != 0 and mineSweeper.tiles[y][x] != constant('App\\MineSweeper::BOMB') ? mineSweeper.tiles[y][x] }}
            </a>
        {% endfor %} 
    {% endfor %} 
    </div>
</body>
</html>